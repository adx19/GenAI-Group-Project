--extra-index-url https://download.pytorch.org/whl/cpu

alembic==1.18.4
fastapi==0.136.3
uvicorn==0.49.0

SQLAlchemy==2.0.50
psycopg2-binary==2.9.12

pydantic==2.13.4
pydantic-settings==2.14.1

python-dotenv==1.2.2
python-multipart==0.0.32

requests==2.34.2
pillow==12.2.0

numpy==1.26.4
pandas

faiss-cpu==1.14.2

# torch: CPU-only slim build to minimize Railway memory footprint.
# The GPU build (default) is ~800 MB installed; CPU-only is ~200 MB.
torch==2.2.2+cpu

# transformers: used by CLIPModel + CLIPProcessor for image search.
# sentence-transformers has been removed — it adds ~100 MB overhead
# and was not the model used to build the FAISS index.
transformers==4.40.0

google-generativeai==0.8.6

scikit-learn
scipy
